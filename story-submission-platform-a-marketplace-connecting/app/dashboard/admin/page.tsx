import { AlertTriangle, CheckCircle2, ShieldCheck, Users } from "lucide-react";
import { Card } from "@/components/ui/card";
import { DashboardHeader, MetricCard } from "@/components/dashboard-shell";
const rows = ["Production house verification", "Reported synopsis review", "Featured story nomination", "Suspicious contact request"];
export default function AdminDashboard() { const bars = [80,120,90,160,140,190,210,180,240,230]; return <div className="mx-auto max-w-7xl px-4 py-12 sm:px-6 lg:px-8"><DashboardHeader eyebrow="Admin command" title="Moderate trust, safety, and marketplace quality." copy="Verify professionals, manage categories, review reports, feature stories, and audit sensitive actions." /><div className="grid gap-5 md:grid-cols-4"><MetricCard label="Users" value="18.2K" detail="1.9K professionals" /><MetricCard label="Stories" value="12.8K" detail="284 pending review" /><MetricCard label="Reports" value="31" detail="7 high priority" /><MetricCard label="Requests" value="4.7K" detail="82% approved" /></div><div className="mt-8 grid gap-6 lg:grid-cols-[1fr_1fr]"><Card className="p-6"><h2 className="mb-5 flex items-center gap-2 text-xl font-bold"><ShieldCheck /> Verification Queue</h2><div className="space-y-3">{rows.map((row, i) => <div key={row} className="flex items-center justify-between rounded-2xl bg-muted p-4"><span>{row}</span>{i % 2 ? <AlertTriangle className="text-gold" /> : <CheckCircle2 className="text-primary" />}</div>)}</div></Card><Card className="p-6"><h2 className="mb-5 flex items-center gap-2 text-xl font-bold"><Users /> Daily Users</h2><div className="flex h-72 items-end gap-3">{bars.map((h, i) => <div key={i} className="flex-1 rounded-t-xl bg-electric/80" style={{ height: h }} />)}</div></Card></div></div>; }

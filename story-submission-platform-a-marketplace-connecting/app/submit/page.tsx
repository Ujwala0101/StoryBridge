import { Check, ImagePlus, Tags } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
const fields = ["Title", "Genre", "Language", "Story Type", "Logline", "Synopsis", "Mood", "Audience", "Age Rating"];
export const metadata = { title: "Submit Story" };
export default function SubmitPage() { return <div className="mx-auto max-w-6xl px-4 py-12 sm:px-6 lg:px-8"><Badge>Writer studio</Badge><h1 className="mt-4 text-4xl font-black tracking-tight">Submit a protected story pitch.</h1><div className="mt-8 grid gap-6 lg:grid-cols-[260px_1fr]"><Card className="h-fit p-5"><div className="space-y-3 text-sm font-semibold"><div className="rounded-xl bg-primary p-3 text-primary-foreground">1. Basic details</div><div className="rounded-xl bg-muted p-3">2. Overview</div><div className="rounded-xl bg-muted p-3">3. Tags and rights</div><div className="rounded-xl bg-muted p-3">4. Preview</div></div></Card><Card className="p-6"><div className="grid gap-4 md:grid-cols-2">{fields.map((field) => <label key={field} className="space-y-2 text-sm font-semibold"><span>{field}</span><input className="min-h-12 w-full rounded-2xl border border-border bg-background px-4 outline-none focus:ring-2 focus:ring-primary" placeholder={field} /></label>)}</div><label className="mt-5 flex min-h-24 items-center justify-center gap-3 rounded-2xl border border-dashed border-border bg-muted text-sm font-semibold text-muted-foreground"><ImagePlus size={18} />Upload cinematic cover image</label><label className="mt-5 flex items-start gap-3 rounded-2xl bg-muted p-4 text-sm text-muted-foreground"><input type="checkbox" className="mt-1" />I confirm this is my original work and StoryBridge will never expose the full story, script, PDF, or downloadable files publicly.</label><div className="mt-6 flex flex-wrap gap-3"><Button variant="gold"><Check size={16} />Publish protected pitch</Button><Button variant="secondary"><Tags size={16} />Save draft</Button></div></Card></div></div>; }

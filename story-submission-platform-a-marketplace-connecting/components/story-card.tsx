import Image from "next/image";
import { Bookmark, Eye, Heart, LockKeyhole, MessageSquare } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { ButtonLink } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { formatNumber } from "@/lib/utils";
import type { Story } from "@/types";
export function StoryCard({ story }: { story: Story }) { return <Card className="group overflow-hidden transition duration-300 hover:-translate-y-1 hover:shadow-glow"><div className="relative aspect-[16/10] overflow-hidden"><Image src={story.cover} alt="" fill className="object-cover transition duration-500 group-hover:scale-105" sizes="(min-width: 1024px) 33vw, 100vw" /><div className="absolute inset-0 bg-gradient-to-t from-slate-950/76 via-slate-950/10 to-transparent" /><Badge className="absolute left-4 top-4 border-white/20 bg-white/16 text-white backdrop-blur">{story.genre}</Badge></div><div className="space-y-4 p-5"><div><div className="mb-2 flex items-center justify-between gap-3 text-xs text-muted-foreground"><span>{story.type} · {story.language}</span><span>{story.postedAt}</span></div><h3 className="text-xl font-bold tracking-tight">{story.title}</h3><p className="mt-2 line-clamp-2 text-sm leading-6 text-muted-foreground">{story.logline}</p></div><p className="line-clamp-3 text-sm leading-6 text-muted-foreground">{story.synopsis}</p><div className="flex flex-wrap items-center gap-3 text-xs text-muted-foreground"><span className="inline-flex items-center gap-1"><Eye size={14} />{formatNumber(story.views)}</span><span className="inline-flex items-center gap-1"><Heart size={14} />{formatNumber(story.likes)}</span><span className="inline-flex items-center gap-1"><Bookmark size={14} />{formatNumber(story.bookmarks)}</span><span className="inline-flex items-center gap-1 text-gold"><LockKeyhole size={14} />Protected</span></div><div className="flex items-center justify-between gap-3 border-t border-border pt-4"><div className="flex items-center gap-3"><Image src={story.writer.avatar} alt={story.writer.name} width={34} height={34} className="rounded-full" /><span className="text-sm font-semibold">{story.writer.name}</span></div><ButtonLink href={"/stories/" + story.id} size="sm" variant="gold"><MessageSquare size={14} />Request</ButtonLink></div></div></Card>; }
